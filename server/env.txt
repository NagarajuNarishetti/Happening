# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=happeningdb
DB_USER=happeninguser
DB_PASSWORD=happeningpass

# Keycloak Configuration
KEYCLOAK_SERVER_URL=http://localhost:8080
KEYCLOAK_REALM=happening
KEYCLOAK_CLIENT_ID=happening-server
KEYCLOAK_CLIENT_SECRET=CHANGEME
KEYCLOAK_ADMIN_USER=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# Server Configuration
PORT=5000
NODE_ENV=development
REDIS_URL=redis://:happeningpass@localhost:6379
RABBITMQ_URL=amqp://happening:happening@localhost:5672
